<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>

    <!-- Payment -->
        <record id="view_vendor_payment_check_form" model="ir.ui.view">
            <field name="name">account.voucher.form.inherit</field>
            <field name="model">account.voucher</field>
            <field name="inherit_id" ref="account_voucher_payline.view_vendor_payment_form"/>
            <field name="arch" type="xml">
                <page string="Payment Information" position="after" >
                    <page string="Checks" attrs="{'invisible':[('check_type','=', False)]}">
                        <field name="check_type" invisible="1"/>
                        <field name="delivered_third_check_ids" attrs="{'invisible':[('check_type','!=', 'third')]}" widget="many2many" domain="[('type','=','third'),('state', '=', 'holding'),('third_handed_voucher_id', '=', False),('journal_id', '=', journal_id)]" context="{'default_type':'third'}"/> 
                        <field name="issued_check_ids" attrs="{'invisible':[('check_type','!=', 'issue')]}" context="{'default_type':'issue','from_voucher':True,'default_journal_id':journal_id}"/> 
                    </page>
                </page>
                <field name="journal_id" position="after">
                    <field name="checks_amount" widget="monetary" options="{'currency_field': 'currency_id'}" attrs="{'invisible':[('check_type','=', False)]}"/>
                </field>
                <field name="net_amount" position="attributes">
                    <attribute name="attrs">{'invisible':[('check_type','!=', False)]}</attribute>
                </field>
            </field>
        </record> 

   <!-- Receipt -->
        <record id="view_vendor_receipt_check_form" model="ir.ui.view">
            <field name="name">account.voucher.form.receipt.inherit</field>
            <field name="model">account.voucher</field>
            <field name="inherit_id" ref="account_voucher_payline.view_vendor_receipt_form"/>
            <field name="arch" type="xml">
                <page string="Payment Information" position="after" >
                    <page string="Checks" attrs="{'invisible':[('check_type','!=', 'third')]}">
                        <field name="check_type" invisible="1"/>
                        <field name="received_third_check_ids"/> 
                    </page>
                </page>
                <field name="journal_id" position="after">
                    <field name="checks_amount" widget="monetary" options="{'currency_field': 'currency_id'}" attrs="{'invisible':[('check_type','=', False)]}"/>
                </field>
                <field name="net_amount" position="attributes">
                    <attribute name="attrs">{'invisible':[('check_type','!=', False)]}</attribute>
                </field>
            </field>
        </record>
    </data>
</openerp>
